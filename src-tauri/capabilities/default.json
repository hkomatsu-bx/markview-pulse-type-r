{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "メインウィンドウの権限。ファイル選択／保存ダイアログ・イベント受信・現在のファイルをOS既定アプリで開く操作を許可する。",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "dialog:default",
    "dialog:allow-save",
    {
      "identifier": "opener:allow-open-path",
      "comment": "「エディタで開く」は常にアクティブタブの .md / .markdown のみを開く。全パス許可（**）にすると、サニタイズを突破した XSS から任意の実行ファイルを OS 既定アプリで起動できてしまうため拡張子で絞る。照合は大文字小文字を区別しない（tauri の fs スコープが glob::MatchOptions::default() を使うため）ので .MD も通る。",
      "allow": [{ "path": "**/*.md" }, { "path": "**/*.markdown" }]
    }
  ]
}
